from urllib.request import urlopen
from bs4 import BeautifulSoup

html = urlopen("https://www.tfrrs.org/results/70848/4344849/WIAC_Tri/Men's_Shot_Put/")
soup=BeautifulSoup(html.read());
resultsTable=soup.find("tbody")
resultsAndNames=resultsTable.find_all("tr")
namesRows = []
marksRows = []
a=0
for i in resultsAndNames:
    print(resultsAndNames[a].find("a"))
    if a%2==0:
        if "Wis_Stevens_Point" in resultsAndNames[a].find("a")["href"]:
            namesRows.append(resultsAndNames[a])
            b=a+1
            marksRows.append(resultsAndNames[b])
    a=a+1

print(namesRows[0].find("a").getText())

print(marksRows[0].prettify())

for i in namesRows
marksRows[0]

if "Wis_Stevens_Point" not in namesRows[0].find("a"):
    print ("LOL")

marksRows[0].findAll("li")

marks=[]
championMark=0
a=0
for each in marksRows:
    marks=marksRows[a].findAll("li")
    b=0
    for eachMark in marks:
        if marks[b].getText()!="FOUL":
            if int(marks[b].getText())>championMark:
                championMark=int(marks[b].getText())
        print(championMark)
        b=b+1
    a=a+1
    print(one)

for eachMark in marks:
    if "FOUL" in marks[b].getText:
        if marks[b].getText>championMark:
            championMark=marks[b].getText
    print(championMark)
    b=b+1



text=resultsTable.prettify()
f = open(r"C:\Users\akopp\Desktop\print\track\results.txt", "a", encoding="utf-8")
f.write(text)
f.close()